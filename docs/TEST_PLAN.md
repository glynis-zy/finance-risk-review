# 财务单据智能风险审核系统 — 完整功能测试方案

**测试目标**：覆盖登录、单据全生命周期、风险分析（含 LLM 润色）、审批流、对话、规则/流程配置、管理后台、供应商风险、审计日志。

**测试附件**（见 `data/test-fixtures/`）：

| 文件名 | 用途 | 关键字段 |
|---|---|---|
| `invoice_sample.png` | 正常发票 | 销售方：恒通科技；价税合计：¥5000.00；开票日期：2026-08-18 |
| `invoice_mismatch_sample.png` | 金额异常发票 | 销售方：恒通科技；价税合计：¥8000.00 |
| `contract_sample.png` | 采购合同 | 乙方：恒通科技；合同总额：¥50000；付款比例：10%；计划付款：2026-08-25 |
| `itinerary_sample.png` | 差旅行程单 | 北京 2026-08-15 至 08-16（含周末）；交通/住宿/餐费明细 |

> 提示：OCR 依赖百度 key；若 `.env` 未配置 `OCR_API_KEY/OCR_SECRET_KEY`，附件「解析」会回退为预制样例，但**不影响上传、保存、风险分析流程本身**。

---

## 一、登录与首页

### TC01 多角色登录
| 项目 | 内容 |
|---|---|
| 前置条件 | 服务已部署并可访问 `http://82.156.118.135/` |
| 操作步骤 | 1. 打开首页<br>2. 分别用以下账号登录、退出、再换账号 |
| 测试数据 | `zhangsan/123456`（申请人）、`wangwu/123456`（审批人）、`zhaoliu/123456`（财务）、`admin/123456`（管理员） |
| 预期结果 | 每个账号都能进入工作台；左侧菜单根据 `permission_codes` 动态显示；页脚显示姓名与角色 |

### TC02 审核工作台看板
| 项目 | 内容 |
|---|---|
| 操作步骤 | 用 `zhangsan` 登录后停留在「审核工作台」 |
| 预期结果 | 显示「我的单据」「我的审批待办」「高风险报告」「审批中单据」4 个统计；下方「最近单据」表格正常渲染 |

---

## 二、单据管理（列表/查询）

### TC03 单据列表与筛选
| 项目 | 内容 |
|---|---|
| 操作步骤 | 1. 登录 `zhangsan`<br>2. 进入「单据管理」<br>3. 分别按「单据编号」「类型」「状态」筛选，点查询 |
| 预期结果 | 列表按条件刷新；未填条件时显示全部；无结果时显示「暂无」 |

### TC04 新建单据（对公付款单 — 干净用例）
| 项目 | 内容 |
|---|---|
| 操作步骤 | 1. 「新建单据」→ 选「对公付款单」<br>2. 填写基础字段与类型专属字段<br>3. 添加 1 条明细<br>4. 上传 `contract_sample.png`（合同）、`invoice_sample.png`（发票）<br>5. 点「保存并提交」 |
| 测试数据 | 申请部门=市场部；预算部门=市场部；收款单位=恒通科技有限公司；收款账号=6222021000000001；费用类别=技术服务；总金额=5000；申请日期=2026-08-18；合同编号=C2026-TEST-001；供应商名称=恒通科技有限公司；付款比例=10；计划付款日期=2026-08-25 |
| 预期结果 | 保存成功，进入单据详情；状态=`pending_review`；两个附件显示在「附件解析」页 |

### TC05 新建单据（预付款单）
| 项目 | 内容 |
|---|---|
| 操作步骤 | 新建「预付款单」，只上传 `contract_sample.png` |
| 测试数据 | 供应商=恒通科技有限公司；合同编号=C2026-TEST-001；付款比例=30；计划付款日期=2026-08-25；总金额=15000 |
| 预期结果 | 保存成功；预付款单允许仅有合同附件 |

### TC06 新建单据（批量付款单）
| 项目 | 内容 |
|---|---|
| 操作步骤 | 新建「批量付款单」，添加 2-3 条明细，上传 `contract_sample.png`（作为付款依据） |
| 测试数据 | 批次说明=8 月供应商付款；付款笔数=2；明细金额合计=总金额 |
| 预期结果 | 保存成功；明细合计与总金额一致 |

### TC07 新建单据（费用报销单）
| 项目 | 内容 |
|---|---|
| 操作步骤 | 新建「费用报销单」，上传 `invoice_sample.png` |
| 测试数据 | 总金额=5000；添加一条明细「办公用品 5000」 |
| 预期结果 | 保存成功 |

### TC08 新建单据（差旅报销单）
| 项目 | 内容 |
|---|---|
| 操作步骤 | 新建「差旅报销单」，上传 `itinerary_sample.png` 和 `invoice_sample.png` |
| 测试数据 | 出差地点=北京；出差开始=2026-08-15；出差结束=2026-08-16；交通费=2400；住宿费=1500；餐费=300；补贴=0；总金额=4200 |
| 预期结果 | 保存成功；差旅单专属字段必填校验通过 |

---

## 三、单据详情与风险分析（核心验证）

### TC09 金额核对页
| 项目 | 内容 |
|---|---|
| 操作步骤 | 进入 TC04 新建的对公付款单详情 → 点「金额核对」 |
| 预期结果 | 单据总金额=5000；明细合计=5000；发票合计=5000；合同金额=50000；单据-发票差异=0 |

### TC10 风险分析 — 干净用例（验证 LLM 润色）⭐
| 项目 | 内容 |
|---|---|
| 操作步骤 | 在 TC04 单据详情 →「风险分析」→「发起分析」，等待 5-15 秒 |
| 预期结果 | 1. 整体风险等级为 `low` 或 `medium`<br>2. 风险项表格正常渲染<br>3. **展开「查看报告全文」能看到通顺中文 Markdown 段落（非 JSON）** → 证明 AutoDL Qwen3-8B 调通 |

### TC11 风险分析 — 黑名单命中
| 项目 | 内容 |
|---|---|
| 操作步骤 | 新建「对公付款单」，供应商名称填 **远东贸易有限公司**，其他正常，保存后发起风险分析 |
| 测试数据 | 供应商名称=远东贸易有限公司；合同编号=C2026-BLACK；付款比例=30；计划付款日期=2026-08-25；总金额=5000；上传 `contract_sample.png` |
| 预期结果 | 风险报告中出现 **HIGH 等级** 风险项：「供应商远东贸易有限公司处于黑名单」 |

### TC12 风险分析 — 发票金额差异命中
| 项目 | 内容 |
|---|---|
| 操作步骤 | 新建「对公付款单」，总金额填 5000，但上传 `invoice_mismatch_sample.png`（发票金额 8000），发起分析 |
| 测试数据 | 总金额=5000；发票附件=invoice_mismatch_sample.png；合同附件=contract_sample.png |
| 预期结果 | 风险报告中出现「单据申请金额 5000 与发票含税合计 8000 差异...」相关风险项 |

### TC13 风险分析 — 差旅异常命中
| 项目 | 内容 |
|---|---|
| 操作步骤 | 进入 TC08 新建的差旅单 →「风险分析」→「发起分析」 |
| 测试数据 | 出差日期含 2026-08-15（周六）、08-16（周日） |
| 预期结果 | 风险报告中出现「周末消费日期」或「差旅异常」相关风险项 |

### TC14 风险项复核
| 项目 | 内容 |
|---|---|
| 操作步骤 | 用 `admin` 或带 `analysis:review` 权限的账号打开已有风险报告 → 修改风险项状态 → 提交复核 |
| 预期结果 | 风险项状态更新；复核结论和意见保存成功 |

---

## 四、附件解析

### TC15 解析与重新解析附件
| 项目 | 内容 |
|---|---|
| 操作步骤 | 进入任意单据详情 →「附件解析」→ 对附件点「解析」；若已解析则再次点「解析」 |
| 预期结果 | 解析状态变化；若未配置百度 key，解析结果来自预制样例（正常行为） |

### TC16 删除附件
| 项目 | 内容 |
|---|---|
| 操作步骤 | 在「附件解析」页删除一个附件 |
| 预期结果 | 附件从列表消失；数据库记录删除 |

### TC17 下载附件
| 项目 | 内容 |
|---|---|
| 操作步骤 | 点「下载/预览」 |
| 预期结果 | 浏览器下载原图文件 |

---

## 五、审批流

### TC18 审批通过
| 项目 | 内容 |
|---|---|
| 操作步骤 | 1. 用 `zhangsan` 提交 TC04 单据<br>2. 退出，用 `wangwu` 登录 →「审批待办」→ 点「通过」<br>3. 退出，用 `zhaoliu` 登录 →「审批待办」→ 点「通过」 |
| 预期结果 | 单据状态依次变化：`pending_review` → `approved`；每个审批人审批后待办消失 |

### TC19 审批退回
| 项目 | 内容 |
|---|---|
| 操作步骤 | 新建并提交一张单据 → `wangwu` 点「退回」 |
| 预期结果 | 单据状态=`returned`；`zhangsan` 可在「单据管理」中编辑并重新提交 |

### TC20 审批驳回
| 项目 | 内容 |
|---|---|
| 操作步骤 | 新建并提交一张单据 → `wangwu` 点「驳回」 |
| 预期结果 | 单据状态=`rejected`；无法再编辑提交 |

### TC21 撤回单据
| 项目 | 内容 |
|---|---|
| 操作步骤 | `zhangsan` 提交一张单据（尚未被审批）→ 在「单据管理」点「撤回」 |
| 预期结果 | 单据状态回到 `draft` |

### TC22 作废单据
| 项目 | 内容 |
|---|---|
| 操作步骤 | `zhangsan` 在「单据管理」对一张 `draft` 或 `pending_review` 状态单据点「作废」 |
| 预期结果 | 单据状态=`voided` |

### TC23 复制单据
| 项目 | 内容 |
|---|---|
| 操作步骤 | 在「单据管理」或详情页触发复制（需前端支持或直接用接口 POST `/documents/{id}/copy`） |
| 预期结果 | 生成一张新的 draft 单据，字段与原单一致 |

---

## 六、智能审核对话

### TC24 对话定位单据
| 项目 | 内容 |
|---|---|
| 操作步骤 | 登录任意账号 →「智能审核对话」→ 输入「对公付款单 <实际编号>」 |
| 预期结果 | 系统返回该单据的风险解读（可能引用最新分析报告） |

### TC25 对话无编号闲聊
| 项目 | 内容 |
|---|---|
| 操作步骤 | 输入「你好」或「系统有哪些单据类型」 |
| 预期结果 | 系统给出通用回复，不报错 |

---

## 七、规则与流程配置

### TC26 风险规则开关与阈值
| 项目 | 内容 |
|---|---|
| 操作步骤 | 用 `admin` 登录 →「规则配置」→ 找一个启用规则关闭 → 改一个阈值配置 → 保存 |
| 预期结果 | 保存成功；回到单据重新分析时该规则不再命中/按新阈值命中 |

### TC27 审批流程新建与编辑
| 项目 | 内容 |
|---|---|
| 操作步骤 | 用 `admin` 登录 →「流程配置」→「新建流程」→ 填写名称、类型、最小金额、节点（每行 节点名\|角色）→ 保存；再点「编辑」修改 |
| 测试数据 | 流程名=对公付款大于1万；类型=对公付款单；最小金额=10000；节点：`部门经理\|approver` `财务复核\|finance` |
| 预期结果 | 新建/编辑成功；列表中节点顺序正确 |

---

## 八、供应商风险

### TC28 查看供应商风险
| 项目 | 内容 |
|---|---|
| 操作步骤 | 1. 新建对公付款单，供应商=远东贸易有限公司<br>2. 进入单据详情 → 在「基本信息」页点「查看供应商风险」 |
| 预期结果 | 跳转到供应商风险页，显示：黑名单状态、风险标签「黑名单」、历史付款 |

### TC29 查看正常供应商
| 项目 | 内容 |
|---|---|
| 操作步骤 | 供应商=恒通科技有限公司，点「查看供应商风险」 |
| 预期结果 | 显示正常、无风险标签 |

---

## 九、审核记录与报告

### TC30 历史风险报告列表
| 项目 | 内容 |
|---|---|
| 操作步骤 | 用 `admin` 登录 →「审核记录」 |
| 预期结果 | 上方显示所有风险报告列表，含风险等级、建议、生成时间；可导出 HTML |

### TC31 导出风险报告
| 项目 | 内容 |
|---|---|
| 操作步骤 | 在「风险分析」页或「审核记录」页点「导出报告(HTML)」 |
| 预期结果 | 浏览器下载 HTML 文件，内容包含风险报告 Markdown |

### TC32 操作审计日志
| 项目 | 内容 |
|---|---|
| 操作步骤 | 「审核记录」页下方「操作审计日志」 |
| 预期结果 | 能看到 submit/approve/return/reject/analysis 等操作的时间、用户、动作、资源 |

---

## 十、系统管理

### TC33 用户管理
| 项目 | 内容 |
|---|---|
| 操作步骤 | 用 `admin` 登录 →「系统管理」→「用户管理」→ 新建用户 → 编辑角色 → 停用/启用 |
| 测试数据 | 用户名=test_user01；姓名=测试用户；角色=applicant |
| 预期结果 | 新建成功；角色正确；停用后无法登录 |

### TC34 角色权限配置
| 项目 | 内容 |
|---|---|
| 操作步骤 | 「系统管理」→「角色权限」→ 修改某角色权限 → 保存 |
| 预期结果 | 保存成功；被修改用户重新登录后菜单与接口权限生效 |

### TC35 系统参数
| 项目 | 内容 |
|---|---|
| 操作步骤 | 「系统管理」→「系统参数」→ 修改某个参数值 → 保存 |
| 预期结果 | 提示「已保存，生效于下一次分析」；再次分析时参数生效 |

---

## 十一、边界与异常

### TC36 必填校验
| 项目 | 内容 |
|---|---|
| 操作步骤 | 新建对公付款单时留空「供应商名称」「付款比例」等必填字段 |
| 预期结果 | 保存失败，弹窗提示具体错误字段 |

### TC37 差旅日期校验
| 项目 | 内容 |
|---|---|
| 操作步骤 | 新建差旅报销单，出差开始日期晚于结束日期 |
| 预期结果 | 保存失败，提示「出差开始日期不能晚于结束日期」 |

### TC38 未登录访问
| 项目 | 内容 |
|---|---|
| 操作步骤 | 清除浏览器 localStorage 后访问 `#/documents` 或调用 API |
| 预期结果 | 自动回到登录页；接口返回 401 |

### TC39 越权访问
| 项目 | 内容 |
|---|---|
| 操作步骤 | 用普通申请人账号直接访问 admin 菜单（若前端隐藏，可通过改 hash 或 curl 试） |
| 预期结果 | 后端返回 403，前端显示无权限 |

---

## 十二、后端接口冒烟（curl 版，可选）

在服务器上可直接跑：

```bash
# 1. 登录拿 token
TOKEN=$(curl -s -X POST http://127.0.0.1:8000/api/v1/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"username":"zhangsan","password":"123456"}' | \
  python3 -c "import sys,json;print(json.load(sys.stdin)['access_token'])")

# 2. 单据列表
curl -s http://127.0.0.1:8000/api/v1/documents -H "Authorization: Bearer $TOKEN" | head -c 200

# 3. 健康检查
curl -sf http://127.0.0.1:8000/health
```

---

## 执行建议

1. 把本页打印或另开一屏，逐项勾选。
2. 优先跑 TC01-TC05、TC10（干净用例验证 LLM）、TC11（黑名单命中）、TC18（审批流）。这四项若全绿，系统主干已通。
3. 其余模块按业务相关度逐步补齐。
4. 每个失败项截图「错误弹窗 + 浏览器 Network 面板」给我，可快速定位。
